<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body> -->
<%- include ('partials/header.ejs') -%>
    <h1>Home</h1>
    <p> <%= homeBody %> </p>
    <% posts.forEach(post => { %>
     <h1><%= post.compositionTitle %></h1> 
     <%if(post.compositionBody.length>100){%>
        <p><%= post.compositionBody.substring(0, 100) + '... ';%> 
        <a href="/posts/<%=post.compositionTitle%>">Continue Reading</a>
        </p>
    <%}else{%>
     <p><%= post.compositionBody %></p> 
    <% }}) %>
   
    <!-- for( let i = 0; i < posts.length; i++){ -->
        <!-- console.log(posts[i].compositionTitle)  -->
        <!-- <h1>   posts[i].compositionTitle %>  </h1>   -->
        <!-- <p>  posts[i].compositionBody %> </p>  -->
        <!--  } %> -->
<%-include ('partials/footer.ejs')-%>
<!-- 
</body>
</html> -->
